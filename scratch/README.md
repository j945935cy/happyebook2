# Scratch Workspace

這個資料夾放一次性整理、抓取與轉換用的輔助檔案，不屬於正式 GitHub Pages 網站路徑。

- `scripts/`：維護或抓取用 Python 腳本。
- `data/`：腳本輸出的暫存資料、原始 HTML 與解析結果。

正式網站頁面請優先放在根目錄、`books/`、`articles/`、`mini-projects/`、`assets/` 或 `src/`。
